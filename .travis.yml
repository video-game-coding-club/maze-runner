---

language: node_js

addons:
  apt:
    packages:
      - tiled

cache:
  - npm

install:
  - npm install

jobs:
  include:
    -stage: Lint
    script:
      - npm run lint || true
      - # npm run build
